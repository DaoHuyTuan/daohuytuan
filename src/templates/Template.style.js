import styled from "styled-components"
import { colorTheme } from "../theme/variable";

export const BackButton = styled.div`
    box-shadow: 0 1px 0 0 ${colorTheme};
    color: ${colorTheme}
`
export const NextButton = styled.div`
    box-shadow: 0 1px 0 0 ${colorTheme};
    color: ${colorTheme}
`